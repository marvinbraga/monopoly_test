[project]
name = "monopoly-test"
version = "0.1.0"
description = "Simulação de Banco Imobiliário"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "coverage>=7.15.4",
    "flake8>=7.3.0",
    "ipython>=9.16.1",
    "pylint>=4.0.7",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
]

[tool.uv]
package = false
